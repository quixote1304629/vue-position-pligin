{
  "name": "vue-position-plugin",
  "version": "1.0.0",
  "main": "dist/index.js",
  "files": [
    "dist"
  ],
  "description": "",
  "workspaces": [
    "plugin",
    "ui"
  ],
  "scripts": {
    "build": "npm run build --workspace=ui && npm run build --workspace=plugin",
    "postbuild": "rm -rf dist && mkdir dist && cp ui/dist/index.js dist/vue-position-ui.js && cp -r plugin/dist/* dist/"
  },
  "author": "",
  "license": "ISC",
  "peerDependencies": {
    "webpack": "^5.1.0",
    "html-webpack-plugin": "^5.1.0"
  }
}
